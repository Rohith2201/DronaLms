{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "frontend/dist/drona-lms-frontend",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}